{
  "name": "registration-form-email",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",            // Starts the Node.js server
    "build": "echo 'No build process required'" // Dummy build command, as there's no build process for a backend app
  },
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.0.1",
    "express": "^5.1.0",
    "nodemailer": "^7.0.4"
  }
}
